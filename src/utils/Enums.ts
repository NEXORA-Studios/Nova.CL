export enum EAccountType {
    Online = 0,
    Offline = 1,
    Nide = 2,
    Authlib = 3,
}

export enum ELaunchStatus {
    Downloading = 0,
    Downloaded = 1,
    Launching = 2,
    Launched = 3,
    Failed = 4,
}
